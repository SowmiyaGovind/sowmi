/* const participant={};
function setInObject(object,key,value){
    
    //object[key]=value;
    // return object

    // object.key = value    // its only give first key  of value
    object[key]=value;        // its give dinamic key values
    }
    setInObject(participant, 'name' , 'p')
    setInObject(participant, 'age' , 12)
    setInObject(participant, 'location' , 'chennai')

    console.table(participant) */

