const fill = (arraySize, value) => {
  const new_arr = [];

  for (let i = data; i <= data; i++) {
    result = new_arr.push(valueToFill);
  }

  return new_arr;
};
const data = 3;
const valueToFill = "a";
console.log(fill(data, valueToFill));//[a,a,a]
